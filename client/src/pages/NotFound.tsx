import { Typography } from "@mui/material";
import { MainTemplate } from "../templates/MainTemplate";

export const NotFound = () => {
  return (
    <MainTemplate>
      <Typography>Ooops! We couldn`t find that page.</Typography>
    </MainTemplate>
  );
};
